<!--pages/index.wxml-->
<view class="container">
  <view class="list" wx:for="{{list}}" wx:key="index" bindtap="onTapList" data-tab="{{item.title}}">
    <view class="box"></view>
    <view class="text">{{item.title}}</view>
  </view>
</view>